{% extends 'base.html.twig' %}

{% block title %}Motif{% endblock %}

{% block body %}
    <h1>Motif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ motif.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ motif.designation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('motif_index') }}">back to list</a>

    <a href="{{ path('motif_edit', {'id': motif.id}) }}">edit</a>

    {{ include('motif/_delete_form.html.twig') }}
{% endblock %}
