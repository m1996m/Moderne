{% extends 'base.html.twig' %}

{% block title %}Reinitialiser votre mot de passe{% endblock %}

{% block body %}
    <div class="container">
       <div class="col-md-12"> <h1 style="text-align:center;">Reinitialisation de mot de passe</h1></div>
       <div class="card"> 
      
            {{ form_start(resetForm) }}
            {{ form_row(resetForm.plainPassword) }}
            <button class="btn btn-primary">Reinitialiser</button>
            {{ form_end(resetForm) }}
         </div>
        
    </div>

{% endblock %}
