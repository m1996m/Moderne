{% extends 'base.html.twig' %}

{% block title %}TypeExamen{% endblock %}

{% block body %}
    <h1>TypeExamen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_examan.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ type_examan.designation }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ type_examan.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_examen_index') }}">back to list</a>

    <a href="{{ path('type_examen_edit', {'id': type_examan.id}) }}">edit</a>

    {{ include('type_examen/_delete_form.html.twig') }}
{% endblock %}
