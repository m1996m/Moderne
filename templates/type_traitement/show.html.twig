{% extends 'base.html.twig' %}

{% block title %}TypeTraitement{% endblock %}

{% block body %}
    <h1>TypeTraitement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_traitement.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ type_traitement.designation }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ type_traitement.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_traitement_index') }}">back to list</a>

    <a href="{{ path('type_traitement_edit', {'id': type_traitement.id}) }}">edit</a>

    {{ include('type_traitement/_delete_form.html.twig') }}
{% endblock %}
